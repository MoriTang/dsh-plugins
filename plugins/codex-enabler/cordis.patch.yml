# Companion layer for the official subagent-codex Profile Bundle. The official
# bundle must precede this one so this patch configures its existing Host row
# instead of registering a second provider. Agent Presets grant the
# model-facing tool independently.
- id: subagent-codex
  config:
    providerName: codex
    permissionMode: never
